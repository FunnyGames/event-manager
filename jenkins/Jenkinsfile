pipeline {
    agent none
    stages {
        stage('Build') {
            agent {
                docker {
                    image 'python:3-alpine'
                }
            }
            steps {
                sh 'sudo -H pip install django'
                sh 'sudo -H pip install pymysql'
                sh 'python event_manager/manage.py runserver'
            }
        }
    }
}